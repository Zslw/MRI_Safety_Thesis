The relevant expressions used throughout this work are summarized below for clarity

\begin{figure}[h!]
	\centering
	\fbox{%
		\parbox{0.9\textwidth}{
			\centering
			\textbf{Summary of Relevant Expressions}\\[4pt]
			\(
			\begin{aligned}
				\label{eq:relevantExpressions}
				F_m &= \frac{\chi_m V}{\mu_0} B_0 \frac{dB_0}{dz}, && \text{(Eq.~\ref{eq:translationalForce})} \nonumber \\
				\frac{F_m}{F_g} &= \tan(\alpha), && \text{(Eq.~\ref{eq:deflectionAngle})} \nonumber \\
				\tan(\alpha) &= \frac{\chi_m}{\rho \mu_0 g} B_0 \frac{dB_0}{dz}, && \text{(Eq.~\ref{eq:forFit})} \nonumber \\
				\tau(\theta) &= \tau_{max} \sin(2\theta) ,
				&& \text{(Eq.~\ref{eq:torque_thetadependece})} \nonumber \\
				\tau_{\text{max}} &= \frac{\chi_m B_0^2 V}{2 \mu_0}, && \text{(Eq.~\ref{eq:maxtorque})} \nonumber \\
				\Delta V &= m \Delta W, && \text{(Eq.~\ref{eq:torqueCal})}
			\end{aligned}
			\)
		}
	}
	\caption{Summary of relevant equations used in this study.}
	\label{fig:relevantExpressions}
\end{figure}