\section{Translational Experiment}
%translational

\subsection{Methodology Notes}
% tranlational in lab methodology discussion 

Initial trials attempted to map the magnetic field from the furthest position of the pendulum, 8 cm from the solenoid face, at a constant solenoid current in a single run. The idea was to obtain ($B_0$) at the pendulum’s resting position and then compute the field gradient at any point from that map. Afterward, the pendulum was allowed to be slowly pulled toward the solenoid to its equilibrium position so its displacement (in cm) could be recorded. This was repeated while increasing the solenoid current in 0.5 A steps, aiming to fit the resulting equilibrium positions using Eq. \ref{eq:forFit}. 

These attempts produced inconsistent results that pointed to errors inherent to the setup. Because the current, rather than the entire apparatus, was changed, the value of $B_0$ at the pendulum’s resting position did not correspond to the required by Eq. \ref{eq:forFit}. Even using the mapped field, estimating the correct value for the magnetic field for each equilibrium position was unreliable. Moreover, predicting equilibrium positions before running the experiment proved difficult. Additional complications came from solenoid imperfections, current leakage, and induced currents, all of which became significant once displacements exceeded 1 cm. At higher fields the pendulum was sometimes pulled fully into the solenoid. Another facor was the gaussmeter restriction on accurate motion tracking to roughly 11–12 mm on the translation stage. While this approach resembles the proposed clinical protocol, the scale of the apparatus and the nonuniform fields make it impractical. Clinically, a single mapping session would give both B and $\nabla$B at any point, leaving the angle measurement as the critical step.


\subsection{Clinical Methodology}
% discussion about translational clincal methodology
Initial setup revealed practical challenges with string attachment to the baskets. Knotting proved time-consuming and inconsistent across different basket designs, leading to variability in suspension length and pre-tension. To streamline future sessions, pre-prepared assemblies are recommended: additional 3D-printed baskets should be fabricated, each paired with a fixed-length string segment of approximately 1.3\,m, pre-weighted, and labeled for rapid deployment.

\subsection{F2042-15 criterion and predictions}
%Discussion F2042-15 criterion and predictions
Figure~\ref{fig:comparison_gradient} presents the allowable maximum spatial gradient derived from the ASTM F2052-15 Appendix X3 extrapolation (Eq.~X3.9)\cite{astmF2052}, which projects measured deflection angles at the test location onto the clinical scanner conditions of 1.5 T and 3 T. Angles below 2$^\circ$ are floored to 2$^\circ$ for conservative extrapolation, this floor was applied to the Ti Coil and Ti Rod results. The extrapolated allowable spatial gradients substantially exceed the typical scanner limits reported, namely $19$ T/m for 1.5 T systems and $17$ T/m for 3.0 T systems for all four devices. Of most interest, the bipolar lead yields an allowable gradient of 107 T/m at 1.5 T and 54 T/m at 3.0 T, both far beyond the scanner's physical maximum gradient, indicating that the translational force alone does not pose a safety concern under standard clinical conditions.

% ----------------------------------------------------------
% Figure 3
% ----------------------------------------------------------
\begin{figure}[H]
	\centering
	\includegraphics[width=0.75\textwidth]{Assets/comparison_allowable_gradient.png}
	\caption[Maximum allowable spatial gradient.]{Maximum allowable spatial gradient. The measured test location was $B_{0L} = 2.511 T$, $|\nabla B|_L = 5.961 T/m$ With actual maximum gradient marked in red.}
	\label{fig:comparison_gradient}
\end{figure}

Figure~\ref{fig:comparison_ordinate} breaks down the force ratio by our mapping fixture coordinates (\ref{fig:fieldmapping_b}) for all devices, being coordinate A6 (closer to the bore top) where the deflection would be most pronounced.  The largest deflections occur closest to the bore entrance, which is the expected worst-case location\cite{astmF2052}. And this positional dependence in height aligns with the $B|\nabla B|$ product peaking at the bore entrance's top as established in literature \cite{mriquestions_dangerous_place}, however our calculated values are higher, suggesting further revision.

% ----------------------------------------------------------
% Figure 4
% ----------------------------------------------------------
\begin{figure}[H]
	\centering
	\includegraphics[width=0.85\textwidth]{Assets/comparison_FR_per_coordinate.png}
	\caption[Field map and translational Text Cross analysis.]{Field map and translational Text Cross analysis. Left: Measured $|B \cdot \nabla B|$ per fixture coordinate on the forward pass, sub run B. Right: Predicted Force Ratio ($FR$) per device at each fixture coordinate at bore entrance.}
	\label{fig:comparison_ordinate}
\end{figure}

It should be noted, that per F2052-15 \cite{astmF2052}, a passing result for translational force is not sufficient alone to establish MR safety. RF-induced heating (ASTM F2182 \cite{astmF2182}) and torque (ASTM F2213 \cite{astm2213}) must also be assessed. The Ti Rod and 316L stainless steel samples were tested in a single trial only, falling short of the three-repetition minimum required by the standard. These results should therefore be treated as indicative rather than fully compliant.

\section{Torque Experiment}
%torque
\subsection{Circuit and calibration}
%disscussion on circuit and torque on the lab from results
Detection limit (from calibration):
$\Delta V_{min} = 0.885 mV
F_min  = 247.52 \mu N
\tau_min  = 1.238e-05 N\cdot m$

Short rod at Helmholtz $B_max = 3.85 mT$ ($\chi_m$ from neodymium fit):
$\tau_{expected }= 2.601e-04 N \cdot m
\tau_{min}      = 1.238e-05 N \cdot m
ratio      = 21.02$

Ratio $> 1$, signal above floor in principle, yet no fit emerged for the short rod under Helmholtz coils. 

This is consistent with $\chi_m$ being overestimated (the two rods may differ in grade) and/or the Helmholtz field being near-uniform but at the edge of the regime where the angle assumption ($\theta$=45°) holds. The bridge offset $V_0$=1.14e-5 V is well below the 0.885 mV floor, so the baseline is not the limiting factor here.

** Circuit note (Discussion, from Dr. Rutel, OU): The balancing potentiometer sits on the output side of the amplifier. Integrating a trim resistor as a dedicated Wheatstone bridge arm would achieve null balance within the bridge itself, eliminating the residual offset at the source and improving sensitivity for weak signals.

%discussion on clinical torque
\subsection{Clinical Methodology Notes}

Multiple lever arms of identical size fabricated in advance, each with samples of the test metals pre-attached to ensure consistent mounting and to facilitate rapid swapping during trials would be an easy improvement.

\subsection{F2213-17 criterion and predictions}

Figure~\ref{fig:torque_projection} presents the predicted magnetically induced torque as a function of rod length for three material classes at the fringe field measured during the clinical session ($B \approx 0.5$ T at ${\sim}0.4$ m from the
bore face) and at the assumed nominal isocenter field of 3.0 T.

Solid lines give $\tau_{\rm mag}$ per diameter, dashed lines of the same color give the gravitational reference $\tau_{\rm grav} = mgL$ \cite{astm2213,Woods2021_MRIdisplacement}, as the conservative ASTM F2213-17 acceptance criterion. The gray dotted line marks the sensor detection floor $\tau_{\rm min} = {1.24e-5}{N \cdot m}$ established in Section~\ref{sec:calResult}.

\begin{figure}[htb!]
	\centering
	\includegraphics[width=0.95\textwidth]{Assets/fig_torque_projection.png}
	\caption[Predicted $\tau_{\rm mag}$ vs $\tau_{\rm grav}$ as a function of cylinder length for three material classes]{%
		Predicted $\tau_{\rm mag}$ (solid) and gravitational reference
		$\tau_{\rm grav} = mgL$ (dashed, same colour by diameter) as a function of rod length at 0.5 T (top row) and 3.0 T (bottom row).
		Columns: ferromagnetic calibration rods ($\chi \approx 113$, this work, quadratic fit), 316L stainless steel -  implant-grade ($\chi = 3.27 \times 10^{-3}$, back-calculated from Woods \textit{et al.} [2021]~\cite{Woods2021_MRIdisplacement}), titanium - pure element
		($\chi = 1.807 \times 10^{-4}$, \cite{periodictable_Ti_chi}).
		Grey dotted: sensor floor $\tau_{\rm min}$.
		Purple dash-dot: crossover $L^*$.
		Big star,Diamond: short/long lab cylinder tests, filled~$= \tau_{\rm mag}$,open~$= \tau_{\rm grav}$.}
	\label{fig:torque_projection}
\end{figure}

A solid line lies above its paired dashed line, $\tau_{\rm mag} > \tau_{\rm grav}$ means the ASTM criterion is not met.

The susceptibility of implant-grade 316L was derived from the deflection angle ($\bar{\theta} = 8.8^\circ$) reported by Woods \textit{et al.} [2021] \cite{Woods2021_MRIdisplacement} at a test location with $B = 1.22$ T and $|\nabla B| = 3.78$ T/m, using the ASTM~F2052\cite{astmF2052} force equation.

For titanium, $L^* < 0.1$ cm at both field strengths, so titanium passes trivially regardless of rod dimensions.

Similar to translationa, it should be noted that per ASTM F2213-17~\cite{astm2213}, a single session with limited positioning attempts does not fulfill the standard's full procedural requirements. These observations should be treated as indicative rather than fully compliant.


\section{Final Thoughts on all 3 experiments}
% discussion clinical all 3

Another thing to note is that we could measure all 3 things at the same time. By putting the torque first then the translational then the grid in the MRI couch, we can potentially take all measurements in a single couch run. However, there are some issues with that, first we needed to make sure the field mapping reaches the same points the torque does. Second, a single computer with Labview running will stall by receiving the inputs from both the Gaussmeter and the DAQ since both are set to read from USB. 